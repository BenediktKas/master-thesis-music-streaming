# notebooks/

Exploratory notebooks. Keep heavy compute in `src/`; notebooks are for EDA,
figures, and results. Clear outputs before committing (or use nbstripout) —
`.ipynb_checkpoints/` is gitignored.

`00_eda_shared.ipynb` is ready (run the pipeline first). Add one per member next.
