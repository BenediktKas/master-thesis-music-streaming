# Core
duckdb>=1.0
polars>=1.0
pandas>=2.0
numpy>=1.26
pyarrow>=15.0
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13

# Member A — segmentation / embeddings
umap-learn>=0.5
hdbscan>=0.8

# Member B — sequential deep learning
torch>=2.2

# Member C — survival analysis
lifelines>=0.28
# pycox  # optional, for DeepSurv

# Member D — uplift / causal
econml>=0.15
causalml>=0.15

# Member B baseline / Member E
xgboost>=2.0
